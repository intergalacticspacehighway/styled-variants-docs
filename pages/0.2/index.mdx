import { MyCustomComponent } from "components/MyCustomComponent";

# hey from ${props.lastName}

<MyCustomComponent name={props.lastName} />

export async function getStaticProps() {
  return {
    props: {
      lastName: "world",
    },
  };
}
